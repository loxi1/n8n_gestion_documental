Sí. En process_correo() el cambio principal es que ya no debes validar solo factura/guía por separado, sino usar tu función nueva:
Python
is_documento_valido_produccion(fields, qr_data)
1. Dentro del for doc in enriched, reemplaza esto
Python
es_documento_valido = True
diferencias_criticas = []
advertencias = []

if tipo_documental == "factura":
    es_documento_valido, diferencias_criticas, advertencias = is_factura_valida_produccion(
        fields,
        qr_data,
    )

elif tipo_documental == "guia_remision":
    es_documento_valido, diferencias_criticas, advertencias = is_guia_valida_produccion(
        fields,
        qr_data,
    )
por esto:
Python
es_documento_valido, diferencias_criticas, advertencias = is_documento_valido_produccion(
    fields,
    qr_data,
)
2. En la decisión de estado, incluye orden_compra
Busca algo parecido a:
Python
elif tipo_documental in ("factura", "guia_remision", "adjunto_factura"):
Déjalo así:
Python
elif tipo_documental in ("factura", "guia_remision", "orden_compra", "adjunto_factura"):
3. La regla de estado debe quedar así
Python
if tipo_documental == "otro":
    estado_documento = "no_identificado"
    bucket = "no_identificados"
    estado_archivo = "observado"
    total_no_identificados += 1

elif tipo_documental in ("factura", "guia_remision", "orden_compra", "adjunto_factura"):
    if not es_documento_valido:
        estado_documento = "revision_manual"
        bucket = "pendientes_revision"
        estado_archivo = "observado"
        total_revision_manual += 1

    elif DEV_FORCE_REVIEW_FACTURAS and tipo_documental in ("factura", "adjunto_factura"):
        estado_documento = "revision_manual"
        bucket = "pendientes_revision"
        estado_archivo = "observado"
        total_revision_manual += 1

    elif tipo_documental == "orden_compra":
        estado_documento = "pendiente_asociacion"
        bucket = "pendientes_revision"
        estado_archivo = "observado"
        total_revision_manual += 1

    else:
        estado_documento = "clasificado"
        bucket = "pendientes_clasificados"
        estado_archivo = "renombrado"
        total_clasificados += 1

elif tipo_documental in ("certificado_calidad", "requerimiento_compra", "cotizacion"):
    estado_documento = "pendiente_asociacion"
    bucket = "pendientes_revision"
    estado_archivo = "observado"
    total_revision_manual += 1

else:
    estado_documento = "revision_manual"
    bucket = "pendientes_revision"
    estado_archivo = "observado"
    total_revision_manual += 1
4. Resultado esperado para OC
Con tu función:
Python
if tipo == "orden_compra":
    return fields.get("numero") is not None, [], []
y con classifier.py ya llenando:
Python
serie = "OC"
numero = "007902"
el nombre debe salir así:
Plain text
04-0003 ORDEN_COMPRA OC 007902 20538549071 OMEGA_POWER_S_A_C.pdf
y debe quedar en:
Plain text
pendientes_revision/2026/04/
porque una OC no es documento final, sino documento para asociar.
Ojo importante
Si quieres que la OC también vaya a pendientes_clasificados, cambia este bloque:
Python
elif tipo_documental == "orden_compra":
    estado_documento = "pendiente_asociacion"
    bucket = "pendientes_revision"
por:
Python
elif tipo_documental == "orden_compra":
    estado_documento = "clasificado"
    bucket = "pendientes_clasificados"
    estado_archivo = "renombrado"
    total_clasificados += 1
Pero mi recomendación: OC en pendientes_revision / pendiente_asociacion hasta implementar asociación automática entre correos.