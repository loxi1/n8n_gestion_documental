Sí, tu lectura tiene sentido y el ajuste debe ser pequeño y preciso.
Lo importante
Si una factura electrónica siempre lleva:
serie = 4 caracteres alfanuméricos
primer carácter = F
correlativo = solo dígitos, de 1 a más
entonces el patrón correcto para factura es:
Plain text
SERIE: F[A-Z0-9]{3}
NUMERO: \d{1,8}
Eso significa que ejemplos válidos son:
F231-0101575
FE65-0812829
FF12-7700
Y sí: FF12-7700 debe reconocerse como factura.
Mi conclusión
No hace falta mover toda la lógica.
Solo te recomiendo dejar más explícito el patrón de factura y darle prioridad fuerte por nombre de archivo y por texto con “FACTURA ELECTRÓNICA”.
Ajuste recomendado en classifier.py
1. Define patrones fijos arriba del archivo
Pon esto al inicio:
Python
FACTURA_SERIE_RE = r"F[A-Z0-9]{3}"
FACTURA_NUMERO_RE = r"\d{1,8}"

GUIA_SERIE_RE = r"(?:T[A-Z0-9]{3}|GR[A-Z0-9]{2,3})"
GUIA_NUMERO_RE = r"\d{1,8}"
2. Reemplaza detect_tipo_documental() por esta versión
Python
def detect_tipo_documental(text: str, file_name: str) -> str:
    text_u = normalize_text(text)
    name_u = normalize_text(file_name)

    # 1. Certificado
    if (
        "CERTIFICADO DE CALIDAD" in text_u
        or "CERT. DE CALIDAD" in text_u
        or "CERTIFICADO" in name_u
    ):
        return "certificado_calidad"

    # 2. Factura por nombre fuerte (máxima prioridad)
    if (
        re.search(rf"\b{FACTURA_SERIE_RE}-{FACTURA_NUMERO_RE}\b", name_u)
        or re.search(rf"\b\d{{11}}-\d{{2}}-{FACTURA_SERIE_RE}-{FACTURA_NUMERO_RE}\b", name_u)
    ):
        return "factura"

    # 3. Guía por nombre fuerte
    if (
        re.search(rf"\b{GUIA_SERIE_RE}-{GUIA_NUMERO_RE}\b", name_u)
        or re.search(rf"\b\d{{11}}-\d{{2}}-{GUIA_SERIE_RE}-{GUIA_NUMERO_RE}\b", name_u)
    ):
        return "guia_remision"

    # 4. Guía por título real
    if (
        "GUIA REMISION ELECTRONICA" in text_u
        or "GUIA DE REMISION ELECTRONICA" in text_u
        or re.search(rf"\bNRO\s*{GUIA_SERIE_RE}-{GUIA_NUMERO_RE}\b", text_u)
    ):
        return "guia_remision"

    # 5. Orden de compra
    if (
        "ORDEN DE COMPRA" in text_u
        or "ORDEN DE COMRA" in text_u
        or re.search(r"\bOC[- ]?\d{4,}\b", text_u)
        or re.search(r"\bOC[- ]?\d{4,}\b", name_u)
    ):
        return "orden_compra"

    # 6. Requerimiento
    if (
        "REQUERIMIENTO DE COMPRA" in text_u
        or "REQUERIMIENTO" in text_u
        or re.search(r"\bREQ[- ]?\d{3,}\b", text_u)
        or re.search(r"\bREQ[- ]?\d{3,}\b", name_u)
    ):
        return "requerimiento_compra"

    # 7. Cotización
    if (
        "COTIZACION" in text_u
        or "PROFORMA" in text_u
        or "COTIZACION" in name_u
        or "PROFORMA" in name_u
    ):
        return "cotizacion"

    # 8. Factura por texto
    if (
        "FACTURA ELECTRONICA" in text_u
        or re.search(rf"\bNRO\s*{FACTURA_SERIE_RE}-{FACTURA_NUMERO_RE}\b", text_u)
        or re.search(rf"\b{FACTURA_SERIE_RE}-{FACTURA_NUMERO_RE}\b", text_u)
    ):
        return "factura"

    return "otro"
3. Ajusta la extracción de serie/número de factura
En extract_basic_fields() cambia la parte de factura por esta:
Python
if doc_type == "factura":
    patrones = [
        rf"\b({FACTURA_SERIE_RE})-({FACTURA_NUMERO_RE})\b",
        rf"\bNRO\s*({FACTURA_SERIE_RE})-({FACTURA_NUMERO_RE})\b",
    ]
    for fuente in (text_u, name_u):
        for patron in patrones:
            m = re.search(patron, fuente)
            if m:
                serie = m.group(1)
                numero = m.group(2)
                break
        if serie and numero:
            break
Por qué este ajuste es bueno
Antes
Dependías de patrones dispersos.
Ahora
Dejas una regla cerrada y explícita:
factura = F + 3 alfanuméricos
correlativo = dígitos
Entonces FF12-7700 entra perfecto.
Lo que yo creo que estaba pasando
Seguramente ese PDF de Sonepar tenía muchas menciones a:
OC
términos comerciales
condiciones de venta
y el clasificador pudo desviarse si no priorizaba suficientemente el nombre fuerte de factura.
Con este ajuste, el nombre:
Plain text
20111740438-01-FF12-7700.pdf
debería ganarle a cualquier ruido del cuerpo.
Tu estado actual
Por lo que muestras, ya vas bien:
Clasificados correctos
04-0001 FACTURA F231 0101575 ...
04-0002 FACTURA F231 0101576 ...
Falta corregir
FF12-7700
Eso confirma que estás muy cerca.
Mi recomendación final
Haz solo este ajuste de regex y prioridad.
No cambies más cosas ahora.
Luego prueba solo con ese documento
Si sale como:
Plain text
FACTURA FF12 7700 ...
y va a pendientes_clasificados, entonces ya cerraste esta parte.
Si quieres, en el siguiente mensaje te doy el bloque exacto completo de extract_basic_fields() ya pegable con este ajuste.