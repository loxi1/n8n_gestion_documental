Ya vi el problema exacto en tus archivos.
El error actual no es de PostgreSQL “complicado”
Es porque metiste JavaScript dentro del SQL.
Tu save_documentos.sql termina así:
SQL
... VALUES ( {{$json.id}}, 'pendiente', 'Documento descargado desde n8n' ) return items.map(item => { item.json.documento_id = item.json.id; delete item.json.id; return item; });
Eso está mal. PostgreSQL solo espera SQL, no return items.map(...). Lo confirmé en tu archivo save_documentos.sql. �
GitHub
Y el error anterior de save_correo mostraba exactamente lo mismo: también se le había pegado un return items.map(...) al final del SQL, por eso falló con “near return”. Ese save_correo.sql correcto debe terminar solo en RETURNING id;, como ya está ahora en tu repo. �
GitHub
Regla clave en n8n
Nodo Postgres → solo SQL
Nodo Code → JavaScript
Nodo Edit Fields / Set / Merge → transformación de datos entre nodos
No mezcles ambas cosas.
Qué debes hacer
1. Deja save_correo.sql como está
Ese ya está bien:
SQL
INSERT INTO correos_ingresados (
  message_id,
  remitente_email,
  asunto,
  fecha_correo,
  cantidad_adjuntos,
  estado_correo,
  leido,
  procesado
)
VALUES (
  ... 
)
ON CONFLICT (message_id) DO UPDATE
SET ...
RETURNING id;
Está correcto en tu repo. �
GitHub
2. Corrige save_documentos.sql
Debe quedar solo SQL.
Usa esto:
SQL
INSERT INTO documentos (
    correo_id,
    estado_documento,
    observacion
)
VALUES (
    {{$json.correo_id}},
    'pendiente',
    'Documento descargado desde n8n'
)
RETURNING id;
No agregues nada más.
3. Corrige save_files.sql
Ese también está bien como SQL puro, pero depende de que antes ya existan:
correo_id
documento_id
Y además, ahora como quitaste el hash, cámbialo temporalmente para no depender de hash_sha256 si no lo calculas todavía.
Te sugiero dejarlo así:
SQL
INSERT INTO archivos (
    documento_id,
    correo_id,
    nombre_archivo_original,
    nombre_archivo_actual,
    extension,
    mime_type,
    tamano_bytes,
    hash_sha256,
    ruta_temporal,
    es_principal,
    estado_archivo
)
VALUES (
    {{$json.documento_id}},
    {{$json.correo_id}},
    {{$json.originalFileName ? `'${$json.originalFileName.replace(/'/g, "''")}'` : 'NULL'}},
    {{$json.storedFileName ? `'${$json.storedFileName.replace(/'/g, "''")}'` : 'NULL'}},
    '.pdf',
    {{$json.mimeType ? `'${$json.mimeType.replace(/'/g, "''")}'` : `'application/pdf'`}},
    {{$json.tamano_bytes ? $json.tamano_bytes : 'NULL'}},
    {{$json.hash_sha256 ? `'${$json.hash_sha256}'` : 'NULL'}},
    {{$json.relativePath ? `'${$json.relativePath.replace(/'/g, "''")}'` : 'NULL'}},
    true,
    'descargado'
)
RETURNING id;
La versión de tu repo ya está bastante alineada con esto. �
GitHub
Lo que falta de verdad: pasar los IDs entre nodos
Ahora mismo tu cuello de botella no es SQL. Es el flujo de datos.
Debes hacer esto:
Flujo recomendado
Plain text
Code
→ Write File
→ Guardar_Correo
→ Code (renombrar id a correo_id)
→ Guardar_Documentos
→ Code (renombrar id a documento_id)
→ Merge
→ Guardar_Archivos
Pero mejor aún:
Plain text
Code original
→ Write File
→ Guardar_Correo
→ Code_correo_id
→ Merge_1  ← une Code original + correo_id
→ Guardar_Documentos
→ Code_documento_id
→ Merge_2  ← une Merge_1 + documento_id
→ Guardar_Archivos
Cómo hacer los nodos Code intermedios
Después de Guardar_Correo
Agrega un nodo Code con esto:
JavaScript
return items.map(item => {
  item.json.correo_id = item.json.id;
  delete item.json.id;
  return item;
});
Después de Guardar_Documentos
Agrega otro nodo Code con esto:
JavaScript
return items.map(item => {
  item.json.documento_id = item.json.id;
  delete item.json.id;
  return item;
});
Cómo usar los Merge
Merge 1
Une:
salida de Code original
salida de Code_correo_id
Para que el item tenga:
subject
fromEmail
originalFileName
storedFileName
relativePath
correo_id
Merge 2
Une:
salida de Merge_1
salida de Code_documento_id
Para que el item final tenga además:
documento_id
Y recién ahí pasa a Guardar_Archivos.
Por qué te estaba fallando save_files.sql
Porque entraba algo así:
SQL
VALUES (
  undefined,
  undefined,
  NULL,
  NULL,
  ...
)
Eso pasa cuando el nodo no recibe:
documento_id
correo_id
nombres de archivo
No es culpa de la tabla archivos, sino de que los datos no están unidos todavía.
Sobre RAR, ZIP y el “mar de posibilidades”
Tu preocupación es totalmente válida.
Mi recomendación sigue siendo esta:
Fase actual
solo .pdf
Siguiente fase
.zip
Más adelante
.rar
Porque los correos reales son caóticos y si intentas cubrir todo hoy, te frenas.
Además, n8n sí tiene soporte documentado para compresión/descompresión con ZIP/Gzip mediante el nodo Compression. �
Para RAR, normalmente tocaría resolverlo fuera de n8n o con una imagen personalizada, así que dejarlo para después es la mejor decisión.
GitHub
Qué haría ahora mismo
Corrige save_documentos.sql y deja solo SQL.
Agrega Code_correo_id.
Agrega Code_documento_id.
Usa dos Merge.
Luego prueba Guardar_Archivos.
Tu error actual, resumido
No es que la BD esté mal.
Es que estabas haciendo esto:
SQL + JavaScript mezclados
y faltan merges para conservar correo_id y documento_id
Eso ya tiene solución clara.
Si quieres, el siguiente mensaje te lo doy con el diagrama exacto de nodos en orden y cómo configurar cada Merge para que no te pierdas.