El error ya está claro: n8n bloquea crypto en el Code node por seguridad. La documentación dice que el Code node restringe módulos por defecto y que para permitir módulos built-in debes definir NODE_FUNCTION_ALLOW_BUILTIN; incluso pone crypto como ejemplo explícito. �
n8n Docs +1
Además, como tú activaste:
Environment
N8N_RUNNERS_ENABLED=true
la documentación de n8n dice que, si usas Task Runners, esas variables deben añadirse al runner, no solo al nodo principal. También indica que el modo por defecto del runner es internal. �
n8n Docs +1
Lo que te recomiendo
Para seguir avanzando rápido, haz esto:
Opción A, recomendada ahora
Quita el hash del Code node y deja el hash_sha256 para Python más adelante.
Eso te evita pelearte con permisos y runners justo ahora.
Opción B, si quieres usar crypto en n8n
Agrega esto a tu .env:
Environment
NODE_FUNCTION_ALLOW_BUILTIN=crypto
n8n documenta exactamente esa variable y ese valor para permitir crypto. �
n8n Docs +1
Luego reinicia:
Bash
docker compose down
docker compose up -d
Pero en tu caso concreto
Yo no usaría hash en n8n todavía.
Porque tú ya tienes claro que el siguiente motor fuerte será Python, y ahí el hash SHA-256 es trivial y más natural de calcular. Así mantienes n8n en su papel de:
capturar correo
guardar archivo
registrar metadata
y dejas a Python:
hash
extracción
clasificación
renombrado final
Qué cambiar ahora
En tu code.js, elimina la parte de crypto y del hash.
Deja algo simple así:
JavaScript
const now = new Date();

const year = now.getFullYear();
const month = String(now.getMonth() + 1).padStart(2, '0');
const day = String(now.getDate()).padStart(2, '0');
const hour = String(now.getHours()).padStart(2, '0');
const minute = String(now.getMinutes()).padStart(2, '0');
const second = String(now.getSeconds()).padStart(2, '0');
const ms = String(now.getMilliseconds()).padStart(3, '0');

const stamp = `${year}${month}${day}${hour}${minute}${second}${ms}`;

const output = [];

for (const item of items) {
	const binary = item.binary || {};
	const subject = item.json.subject || '';
	const messageId = item.json.messageId || item.json.message_id || null;
	const date = item.json.date || null;

	let fromEmail = '';
	let fromName = '';

	if (item.json.from?.value?.length) {
		fromEmail = item.json.from.value[0].address || '';
		fromName = item.json.from.value[0].name || '';
	} else if (typeof item.json.from === 'string') {
		fromEmail = item.json.from;
	}

	for (const key of Object.keys(binary)) {
		const file = binary[key];
		const fileName = file.fileName || 'adjunto';
		const lowerName = fileName.toLowerCase();

		if (!lowerName.endsWith('.pdf')) {
			continue;
		}

		const newFileName = `posible_documento_${stamp}.pdf`;

		output.push({
			json: {
				subject,
				fromEmail,
				fromName,
				messageId,
				date,
				originalFileName: fileName,
				storedFileName: newFileName,
				tipoDetectado: 'pendiente',
				filePath: `/files/inbox/${year}/${month}/${newFileName}`,
				relativePath: `inbox/${year}/${month}/${newFileName}`,
				mimeType: file.mimeType || 'application/pdf',
				tamano_bytes: file.fileSize || null,
			},
			binary: {
				data: file,
			},
		});
	}
}

return output;
Y en save_files.sql
Como hash_sha256 es NOT NULL, tienes dos caminos:
Camino 1, más rápido
Permitir temporalmente NULL en hash_sha256:
SQL
ALTER TABLE archivos
ALTER COLUMN hash_sha256 DROP NOT NULL;
Luego Python lo llena después.
Camino 2
Insertar un valor temporal, por ejemplo:
SQL
'pendiente'
pero no me gusta porque no es un hash real.
Mi recomendación final
Haz esto:
quita crypto del Code node
deja hash_sha256 nullable temporalmente
sigue con el flujo n8n
calcula el hash real en Python después
Eso te deja avanzar sin bloquearte.
Sobre ZIP y RAR
Tu observación es muy válida: los correos reales serán caóticos.
Mi línea de trabajo sería:
ahora: solo PDF
después: ZIP
mucho después: RAR
Porque si intentas cubrir PDF + XML + ZIP + RAR + DOCX desde ya, te frenas.
Si quieres, el siguiente paso lo centramos en arreglar save_files.sql con correo_id y documento_id usando Merge, que es el siguiente bloqueo real.