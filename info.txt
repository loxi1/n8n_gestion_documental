Sí, hagamos un regreso limpio.
Lo que muestran esas consultas es:
el documento 4 sí se procesó bien
los documentos 1, 2 y 3 ni siquiera fueron procesados por Python
por eso siguen:
tipo_documental = NULL
nombre_final = NULL
estado_archivo = descargado
en inbox/2026/04/
O sea, el problema no es el renombrado final, sino que esos 3 no entraron al procesamiento del correo 1.
Lo más probable
Tu process_correo() está saliendo por esta condición:
Python
factura_principal = select_factura_principal(enriched)
if not factura_principal:
    print(f"[WARN] correo_id={items[0]['correo_id']} sin factura principal")
    return
y por eso todo el correo 1 queda sin tocar.
Mi recomendación
Sí: borra todo de prueba y vuelve a correr, pero esta vez con una mini validación intermedia.
Plan limpio
1. Limpia BD de prueba
Conserva:
proveedores
clientes_destino
clientes_destino_alias
Limpia:
archivos
documentos
correos_ingresados
2. Limpia carpetas de prueba
Vacía:
storage/files/inbox/
storage/files/pendientes_clasificados/
storage/files/no_identificados/
storage/files/ocr_tmp/
3. Vuelve a correr n8n
4. Antes de correr Python, valida que en BD existan los documentos
5. Corre Python
6. Si vuelve a fallar el correo 1, metemos debug solo en:
enrich_document()
select_factura_principal()
Te dejo el regreso limpio paso a paso
A. SQL para limpiar
Ejecuta esto en PostgreSQL:
SQL
TRUNCATE TABLE archivos RESTART IDENTITY CASCADE;
TRUNCATE TABLE documentos RESTART IDENTITY CASCADE;
TRUNCATE TABLE correos_ingresados RESTART IDENTITY CASCADE;
B. Limpiar carpetas desde Git Bash
Desde la raíz n8n-local:
Bash
rm -rf storage/files/inbox/*
rm -rf storage/files/pendientes_clasificados/*
rm -rf storage/files/no_identificados/*
rm -rf storage/files/ocr_tmp/*
Luego recrea la estructura base si quieres:
Bash
mkdir -p storage/files/inbox
mkdir -p storage/files/pendientes_clasificados
mkdir -p storage/files/no_identificados
mkdir -p storage/files/ocr_tmp
C. Corre n8n
Haz que vuelva a descargar el correo y sus adjuntos.
Luego valida con:
SQL
SELECT id, asunto, remitente_email, fecha_correo, estado_correo
FROM correos_ingresados
ORDER BY id;

SELECT id, correo_id, estado_documento
FROM documentos
ORDER BY id;

SELECT id, documento_id, nombre_archivo_actual, ruta_temporal, estado_archivo
FROM archivos
ORDER BY id;
Si ahí ya aparecen 3 o 4 documentos correctamente, seguimos.
Antes de correr Python, mete este debug
En process_correo(), justo después de:
Python
enriched = [enrich_document(x) for x in items]
agrega temporalmente esto:
Python
print(f"[DEBUG] correo_id={items[0]['correo_id']} enriquecidos:")
for d in enriched:
    print({
        "documento_id": d["documento_id"],
        "archivo": d["nombre_archivo_actual"],
        "tipo": d["fields"]["tipo_documental"],
        "serie": d["fields"]["serie"],
        "numero": d["fields"]["numero"],
        "ruc": d["fields"]["ruc"],
        "oc": d["fields"].get("oc"),
        "cliente_raw": d.get("cliente_raw"),
        "cliente_match": d.get("cliente_match"),
        "fecha": d.get("fecha_emision_norm"),
    })
Y luego, justo después de:
Python
factura_principal = select_factura_principal(enriched)
agrega:
Python
print("[DEBUG] factura_principal =", factura_principal)
Qué buscamos con eso
Queremos ver si para el correo 1 ocurre alguna de estas:
la factura entra como otro
la factura entra como guia
select_factura_principal() no encuentra ninguna factura
enrich_document() está devolviendo campos vacíos
Mi sospecha real ahora
Tu problema no está en build_final_name().
Tu problema probablemente está en una de estas dos:
1. select_factura_principal()
Puede estar filtrando demasiado.
2. enrich_document()
Puede que el OCR o la extracción esté clasificando mal la factura del correo 1.
Qué hacemos ya
Haz este regreso limpio:
truncar tablas
limpiar carpetas
correr n8n
correr Python con el debug que te dejé
pegarme la salida de consola de python procesar_pendientes.py
Con eso ya no adivinamos nada y encontramos exactamente por qué el correo 1 no entra.