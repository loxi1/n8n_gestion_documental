Sí, ya vi el fallo exacto.
Qué pasó
El clasificador sí detectó bien la factura 2:
tipo: factura
serie: F231
número: 0101576
fecha: 2026-04-21
Pero el campo importe salió como texto:
Plain text
1,530.70
y PostgreSQL numeric no acepta ese formato con coma de miles, por eso explotó aquí:
Plain text
invalid input syntax for type numeric: "1,530.70"
Conclusión
La lógica documental sí está funcionando.
El problema ahora es solo de normalización del importe antes de guardar.
Solución correcta
Debes convertir:
1,530.70 → 1530.70
400.20 → 400.20
1.530,70 → 1530.70 si algún día aparece así
Qué hacer
Agrega una función para limpiar montos antes de guardar en BD.
En procesar_pendientes.py, agrega esto
Ponla cerca de normalize_date():
Python
def normalize_amount(value: str | None) -> str | None:
    if not value:
        return None

    raw = str(value).strip()

    # quitar espacios
    raw = raw.replace(" ", "")

    # caso 1: formato US -> 1,530.70
    if "," in raw and "." in raw:
        if raw.rfind(".") > raw.rfind(","):
            raw = raw.replace(",", "")
        else:
            # caso europeo -> 1.530,70
            raw = raw.replace(".", "").replace(",", ".")

    # caso 2: solo comas
    elif "," in raw:
        parts = raw.split(",")
        if len(parts) == 2 and len(parts[1]) in (2, 3):
            # probablemente decimal
            raw = raw.replace(",", ".")
        else:
            # probablemente miles
            raw = raw.replace(",", "")

    # dejar solo números, punto y signo
    raw = re.sub(r"[^0-9.\-]", "", raw)

    return raw or None
Luego úsala al guardar
En process_correo() cambia esto:
Python
"importe": fields["importe"],
por esto:
Python
"importe": normalize_amount(fields["importe"]),
También en mark_documents_as_review()
Cambia esto:
Python
"importe": fields.get("importe"),
por:
Python
"importe": normalize_amount(fields.get("importe")),
Te muestro exactamente dónde
1. En process_correo()
Busca este bloque:
Python
cur.execute(
    SQL_UPDATE_DOCUMENTO,
    {
        "proveedor_id": proveedor_id,
        "cliente_destino_id": cliente_id_final,
        "tipo_documental": tipo_documental,
        "serie": fields["serie"],
        "numero": fields["numero"],
        "ruc": ruc_emisor,
        "razon_social": razon_social_emisor,
        "fecha_emision": doc["fecha_emision_norm"],
        "importe": fields["importe"],
y déjalo así:
Python
cur.execute(
    SQL_UPDATE_DOCUMENTO,
    {
        "proveedor_id": proveedor_id,
        "cliente_destino_id": cliente_id_final,
        "tipo_documental": tipo_documental,
        "serie": fields["serie"],
        "numero": fields["numero"],
        "ruc": ruc_emisor,
        "razon_social": razon_social_emisor,
        "fecha_emision": doc["fecha_emision_norm"],
        "importe": normalize_amount(fields["importe"]),
2. En mark_documents_as_review()
Busca:
Python
"importe": fields.get("importe"),
y cambia por:
Python
"importe": normalize_amount(fields.get("importe")),
Además: por qué viste el archivo ya movido
Porque el flujo hizo esto:
detectó bien
generó nombre
movió el archivo
intentó actualizar BD
falló al guardar importe
Entonces te quedó una inconsistencia parcial:
archivo ya movido
BD no terminada para ese documento
Qué hacer ahora con ese documento
Como el archivo ya quedó en:
Plain text
storage/files/pendientes_clasificados/2026/04/04-0002 FACTURA F231 0101576 20100084172 PROMOTORES_ELECTRICOS_S_A.pdf
yo haría esto después del fix:
Opción práctica
Limpia de nuevo BD y carpetas y vuelve a correr.
Como todavía estás en pruebas, esa es la mejor opción.
Extra recomendado
Para que un error en un documento no detenga todos los demás, más adelante conviene envolver cada correo o cada documento en try/except, pero por ahora primero corrige el monto.
Resumen
El error no fue de clasificación, fue de formato numérico.
Falló por:
Plain text
1,530.70
Debe guardarse como:
Plain text
1530.70
Pega esta función y esos dos cambios:
Python
def normalize_amount(value: str | None) -> str | None:
    if not value:
        return None

    raw = str(value).strip()
    raw = raw.replace(" ", "")

    if "," in raw and "." in raw:
        if raw.rfind(".") > raw.rfind(","):
            raw = raw.replace(",", "")
        else:
            raw = raw.replace(".", "").replace(",", ".")
    elif "," in raw:
        parts = raw.split(",")
        if len(parts) == 2 and len(parts[1]) in (2, 3):
            raw = raw.replace(",", ".")
        else:
            raw = raw.replace(",", "")

    raw = re.sub(r"[^0-9.\-]", "", raw)
    return raw or None
y vuelve a probar.
Con eso ese caso debería pasar bien.