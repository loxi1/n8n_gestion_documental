Sí, esa es la dirección correcta.
Tu regla para facturas debe basarse en dos cosas:
patrones fuertes del documento
completar la razón social desde proveedores por ruc, y si no existe, consultar el API y guardar
Eso es mucho más confiable que intentar leer el nombre del logo.
Recomendación para facturas
Detectarlas así
Debe considerarse factura si se cumple al menos una de estas:
aparece FACTURA ELECTRONICA o FACTURA ELECTRÓNICA
aparece un patrón como:
F002-00000254
F231-0101439
F010-00055937
F001-00002322
Y también aceptar variantes como:
N° F231-0101439
Nro. F010-00055937
Regex recomendado
En classifier.py, para detectar factura:
Python
def is_factura(text: str, original_name: str) -> bool:
    text_u = text.upper()
    name_u = original_name.upper()

    patrones_factura = [
        r"\bF\d{3,4}[- ]\d{3,}\b",
        r"\bN[°º]\s*F\d{3,4}[- ]\d{3,}\b",
        r"\bNRO\.?\s*F\d{3,4}[- ]\d{3,}\b",
    ]

    if "FACTURA ELECTRONICA" in text_u or "FACTURA ELECTRÓNICA" in text_u:
        return True

    for patron in patrones_factura:
        if re.search(patron, text_u, re.IGNORECASE):
            return True
        if re.search(patron, name_u, re.IGNORECASE):
            return True

    return False
Y luego en detect_document_type():
Python
def detect_document_type(text: str, original_name: str) -> str:
    text_u = text.upper()
    name_u = original_name.upper()

    if (
        "ORDEN DE COMPRA" in text_u
        or "ORDEN DE COMRA" in text_u
        or "ORDEN DE COMPRA" in name_u
        or "ORDEN DE COMRA" in name_u
    ):
        return "orden_compra"

    if (
        "REQUERIMIENTO DE COMPRA" in text_u
        or "REQUERIMIENTO" in text_u
        or "REQ" in name_u
    ):
        return "requerimiento_compra"

    if (
        "GUIA DE REMISION" in text_u
        or "GUÍA DE REMISIÓN" in text_u
        or "PUNTO DE PARTIDA" in text_u
        or "PUNTO DE LLEGADA" in text_u
    ):
        return "guia"

    if is_factura(text, original_name):
        return "factura"

    if (
        "NOTA DE CREDITO" in text_u
        or "NOTA DE CRÉDITO" in text_u
        or "NOTA DE DEBITO" in text_u
        or "NOTA DE DÉBITO" in text_u
    ):
        return "nota_credito"

    return "otro"
Extracción de serie y número de factura
En extract_basic_fields() usa esto para factura:
Python
if doc_type == "factura":
    patrones = [
        r"\b(F\d{3,4})[- ](\d{3,})\b",
        r"\bN[°º]\s*(F\d{3,4})[- ](\d{3,})\b",
        r"\bNRO\.?\s*(F\d{3,4})[- ](\d{3,})\b",
    ]
Proveedor: estrategia correcta
Tu idea es muy buena:
orden de prioridad
sacar ruc
buscar en proveedores
si existe, usar:
nombre
direccion
si no existe:
consultar API
insertar en proveedores
usar esos datos
Eso es exactamente lo correcto para tus facturas.
Cómo implementarlo
Nuevo helper en procesar_pendientes.py
En vez de get_or_create_proveedor() actual, haz uno más completo.
Necesitarás
requests
token en .env
.env
Environment
APISPERU_TOKEN=tu_token_aqui
config.py
Agrega:
Python
APISPERU_TOKEN = os.getenv("APISPERU_TOKEN", "")
requirements.txt
Agrega:
Plain text
requests==2.32.3
Nueva función
Puedes ponerla en procesar_pendientes.py o mejor en core/proveedores.py.
Python
import requests
from core.config import APISPERU_TOKEN


def fetch_proveedor_from_api(ruc: str) -> dict | None:
    if not ruc or not APISPERU_TOKEN:
        return None

    url = f"https://dniruc.apisperu.com/api/v1/ruc/{ruc}?token={APISPERU_TOKEN}"

    try:
        resp = requests.get(url, timeout=15)
        if resp.status_code != 200:
            return None

        data = resp.json()
        return {
            "ruc": data.get("ruc"),
            "nombre": data.get("razonSocial"),
            "direccion": data.get("direccion"),
        }
    except Exception:
        return None
Reemplaza get_or_create_proveedor() por esto
Python
def get_or_create_proveedor(ruc: str | None, razon_social: str | None = None, direccion: str | None = None) -> dict | None:
    if not ruc:
        return None

    with get_cursor(commit=True) as (_, cur):
        cur.execute(
            "SELECT id, ruc, nombre, direccion FROM proveedores WHERE ruc = %s",
            (ruc,),
        )
        row = cur.fetchone()
        if row:
            return dict(row)

    api_data = fetch_proveedor_from_api(ruc)

    nombre_final = None
    direccion_final = None

    if api_data:
        nombre_final = api_data.get("nombre")
        direccion_final = api_data.get("direccion")
    else:
        nombre_final = razon_social or "SIN_RAZON_SOCIAL"
        direccion_final = direccion

    with get_cursor(commit=True) as (_, cur):
        cur.execute(
            """
            INSERT INTO proveedores (ruc, nombre, direccion, creado_en, actualizado_en)
            VALUES (%s, %s, %s, NOW(), NOW())
            RETURNING id, ruc, nombre, direccion
            """,
            (ruc, nombre_final, direccion_final),
        )
        row = cur.fetchone()
        return dict(row) if row else None
Y en process_correo()
Donde hoy haces:
Python
proveedor_id = get_or_create_proveedor(ruc_emisor, razon_social_emisor)
haz:
Python
proveedor = get_or_create_proveedor(ruc_emisor, razon_social_emisor)
proveedor_id = proveedor["id"] if proveedor else None

if proveedor and proveedor.get("nombre"):
    razon_social_emisor = proveedor["nombre"]
Así el nombre final del archivo usará el nombre limpio y oficial del proveedor.
Resultado esperado
Con eso, para una factura como:
RUC: 20538549071
Factura Electronica
F002-00000254
te debería quedar algo así:
Plain text
04-0001 FACTURA F002 00000254 20538549071 OMEGA_POWER_S_A_C.pdf
Y para Promotores:
Plain text
04-0002 FACTURA F241 0045334 20100084172 PROMOTORES_ELECTRICOS_S_A.pdf
Mi recomendación final
Sí:
detecta factura por patrón Fxxx-xxxxx
usa proveedores como fuente principal de razón social
usa el API solo como fallback
después actualiza proveedores automáticamente
Eso te va a estabilizar muchísimo el nombre final.
Si quieres, en el siguiente mensaje te dejo el bloque exacto de cambios para classifier.py y procesar_pendientes.py ya integrados, listos para pegar.